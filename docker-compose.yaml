version: '3'
services:
  bot:
    build:
      context: .
      args:
        M1: 'false'
    image: hans:latest
    restart: always
    env_file:
      - .env
    environment:
      NODE_ENV: production
