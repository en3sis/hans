// import { isEnable } from '../../db/giveaway.json'

export const GiveAway = () => {
  if (true) {
    // console.log('enabled')
    // fs.writeFile(
    //   `${path.join(__dirname, '../../db/giveaway.json')}`,
    //   JSON.stringify({ isEnable: !isEnable }),
    //   (r) => {
    //     if (r) console.log('err', r)
    //   }
    // )

    return true
  } else {
    return false
  }
}
